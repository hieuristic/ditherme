# DitherMe

A small but performant C+Vulkan cli tools that make dithering images. Result can be found on my
website: https://hieuristic.github.io/.

## Getting Started
```sh
cmake -B build && cmake --build build && ./bin/ditherme -i <input_folder> -o <output_folder> -t <thumbnail_folder>
```
